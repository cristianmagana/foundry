// Application constants
export const APP_NAME = 'foundry';

// Default values
export const DEFAULT_AUTO_INIT = true;
export const DEFAULT_PRIVATE = false;

// Productionalization defaults
export const DEFAULT_PRODUCTIONALIZE = false;
export const DEFAULT_BRANCH_PROTECTION_TARGET = 'master';

// API rate limiting and batching
export const TEAM_PERMISSION_BATCH_SIZE = 5;
export const SECRET_CREATION_DELAY_MS = 100;
